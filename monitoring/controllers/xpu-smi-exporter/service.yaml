apiVersion: v1
kind: Service
metadata:
  name: xpu-smi-exporter
  namespace: ops-monitoring
  labels:
    app: xpu-smi-exporter
spec:
  clusterIP: None
  selector:
    app: xpu-smi-exporter
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
