apiVersion: apps/v1
kind: Deployment
metadata:
  name: nebula-sync
  namespace: svc-pihole
  labels: { app: nebula-sync }
spec:
  replicas: 1
  selector:
    matchLabels: { app: nebula-sync }
  template:
    metadata:
      labels: { app: nebula-sync }
    spec:
      containers:
        - name: nebula-sync
          image: ghcr.io/lovelaze/nebula-sync:latest
          env:
            - name: PRIMARY
              valueFrom:
                secretKeyRef:
                  name: pihole-secret
                  key: primary_string
            - name: REPLICAS
              valueFrom:
                secretKeyRef:
                  name: pihole-secret
                  key: replicas_string
            - name: TZ
              value: "Europe/Berlin"
            - name: FULL_SYNC
              value: "true"
            - name: RUN_GRAVITY
              value: "true"
            - name: CRON
              value: "0 * * * *"
            - name: NS_DEBUG
              value: "true"
            - name: LOG_LEVEL
              value: "debug"
            - name: CLIENT_SKIP_INVALID_SESSION
              value: "true"
            - name: CLIENT_TIMEOUT_SECONDS
              value: "30"
          resources:
            requests: { cpu: 50m, memory: 64Mi }
            limits: { cpu: 200m, memory: 128Mi }