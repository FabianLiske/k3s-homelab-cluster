apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jirafeau-data
  namespace: svc-jirafeau
  labels:
    recurring-job.longhorn.io/backup-daily: enabled
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn-2r
  resources:
    requests:
      storage: 100Gi
