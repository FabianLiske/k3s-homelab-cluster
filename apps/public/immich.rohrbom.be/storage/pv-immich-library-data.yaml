apiVersion: v1
kind: PersistentVolume
metadata:
  name: immich-library-data-pv
  labels:
    app.kubernetes.io/part-of: immich
spec:
  capacity:
    storage: 10000Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: media-local
  local:
    path: /srv/media-hdd/immich/library
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - media-1
