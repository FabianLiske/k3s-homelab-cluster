apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: svc-jupyter
labels:
  - pairs:
      app: jupyter
      gitops: flux
resources:
  - namespace.yaml
  - jupyter-secret.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - netpol-ingress.yaml
