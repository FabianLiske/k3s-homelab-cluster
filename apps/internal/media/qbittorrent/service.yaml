apiVersion: v1
kind: Service
metadata:
  name: qbittorrent
  namespace: svc-media
  labels:
    app: qbittorrent
spec:
  type: ClusterIP
  selector:
    app: qbittorrent
  ports:
    - name: http
      port: 8080
      targetPort: http