apiVersion: v1
kind: Service
metadata:
  name: bazarr
  namespace: svc-media
  labels:
    app: bazarr
spec:
  type: ClusterIP
  selector:
    app: bazarr
  ports:
    - name: http
      port: 6767
      targetPort: http
