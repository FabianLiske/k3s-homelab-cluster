apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr-config
  namespace: svc-media
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-2r-immediate
  resources:
    requests:
      storage: 2Gi
