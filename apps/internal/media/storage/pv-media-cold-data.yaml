apiVersion: v1
kind: PersistentVolume
metadata:
  name: media-cold-data-pv
  labels:
    app.kubernetes.io/part-of: media-stack
spec:
  capacity:
    storage: 24000Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: media-local
  local:
    path: /srv/media-hdd
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - media-1
