apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-data
  namespace: svc-media
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: media-data-pv
  resources:
    requests:
      storage: 24Ti
