apiVersion: v1
kind: Service
metadata:
  name: radarr
  namespace: svc-media
  labels:
    app: radarr
spec:
  type: ClusterIP
  selector:
    app: radarr
  ports:
    - name: http
      port: 7878
      targetPort: http
