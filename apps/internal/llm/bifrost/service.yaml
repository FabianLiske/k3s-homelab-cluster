apiVersion: v1
kind: Service
metadata:
  name: bifrost
  labels:
    app.kubernetes.io/name: bifrost
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bifrost
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
