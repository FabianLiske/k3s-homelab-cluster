apiVersion: v1
kind: Service
metadata:
  name: open-webui-postgresql
  labels:
    app.kubernetes.io/name: open-webui-postgresql
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: open-webui-postgresql
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
